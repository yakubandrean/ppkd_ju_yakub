import 'package:flutter/material.dart';

class Tugas3DartLooping extends StatelessWidget {
  const Tugas3DartLooping({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Color.fromARGB(255, 228, 114, 114),
        title: Text(
          'Tugas 3 (Dart Looping)',
          style: TextStyle(fontWeight: FontWeight.bold),
        ),
        centerTitle: true,
      ),

      body: Column(
        
      ),
    );
  }
}
